import { Login } from "./forms/Login"
import { RegisterForm } from "./forms/RegisterForm"

export const Menu = () => {

    return (
        <div className="menu"><a href="register">Register</a> or <a href="login">Login</a></div>
    )
}